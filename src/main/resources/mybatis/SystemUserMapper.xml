<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.libbytian.pan.system.mapper.SystemUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.libbytian.pan.system.model.SystemUserModel">
        <id column="user_id" property="userId"/>
        <result column="user_name" property="username"/>
        <result column="user_mobile" property="mobile"/>
        <result column="user_password" property="password"/>
        <result column="user_last_login_time" property="lastLoginTime"/>
        <result column="createTime" property="createTime"/>
        <result column="status" property="status"/>
        <result column="act_time" property="actTime"/>
        <result column="act_range" property="actrange"/>
    </resultMap>


    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
         u.user_id AS userId,
         u.user_name AS username,
         u.user_mobile AS mobile,
         u.user_password AS password,
         u.user_last_login_time AS lastLoginTime,
         u.createTime AS createTime,
         u.status AS status,
         u.act_time AS actTime,
         u.act_range AS actrange
    </sql>


    <!-- 根据用户信息查询用户启用状态的模板list-->
    <select id="selectOneUserModel" parameterType="com.libbytian.pan.system.model.SystemUserModel" resultType="com.libbytian.pan.system.model.SystemUserModel">
        SELECT
        <include refid="Base_Column_List"/>
        FROM
        sys_user u
        <trim prefix="where" prefixOverrides="and|">
            <if test="userId != null and userId != ''">
                AND u.user_id= #{userId}
            </if>
            <if test="username != null and username != ''">
                AND u.user_name =#{username}
            </if>
            <if test="status != null and status != ''">
                AND u.status =#{status}
            </if>

        </trim>
    </select>

</mapper>
